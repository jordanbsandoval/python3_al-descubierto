#!/usr/bin/python3
# concatenando tipo string con el operador (+)
cadena = "hola parce"
concatenado_operador = cadena + " todo bien o que"

# concatenando tipo string con el metodo format() 
# permite integrar las ({}) en la cadena para indicar el numero o el nombre de una variable
print("{} {} como esta".format(cadena, concatenado_operador))

